services:
  - type: static
    name: mood-tracker-ag
    env: static
    buildCommand: npm run build
    publishPath: dist
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
